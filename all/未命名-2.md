```mermaid
文本输入 --> BERT模型 --> [CLS]特征 --> 线性层(t_linear) --> ReLU --> 文本特征权重(txt_q) --> 加权文本特征
图像输入 --> DenseNet201 --> 图像特征 --> 线性层(i_linear) --> ReLU --> 图像特征权重(img_q) --> 加权图像特征
                                                                                                    |
                                                                                                    v
                                                                                              特征融合(+)
                                                                                                    |
                                                                                                    v
                                                                                          全连接层(fc)
                                                                                                    |
                                                                                                    v
                                                                                              分类输出
```